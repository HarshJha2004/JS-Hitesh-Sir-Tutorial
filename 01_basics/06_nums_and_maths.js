const score = 400
// console.log(score);


// explicity define datatype.
    const bankBalance = new Number(250)
    // console.log(bankBalance);

// use methods.
    // console.log(bankBalance.toString().length);
    // console.log(bankBalance.toFixed(2));

// Some extra methods.
    const rsNumberCheck = 1000000
    // console.log(rsNumberCheck.toLocaleString('en-IN'));
     

//+++++++++++++++++++++++++++ Maths +++++++++++++++++++++++++++++++++++++++

    // console.log(Math);
    // console.log(Math.abs(-4));
    // console.log(Math.round(4.3));
    // console.log(Math.ceil(4.2));
    // console.log(Math.floor(4.6));


//The (Math) library is mostly used in random values
    // console.log(Math.random())